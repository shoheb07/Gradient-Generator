# Gradient-Generator
Random Gradient Generator is a simple web application built using HTML, CSS, and JavaScript that generates random linear gradient backgrounds with a single click. It dynamically creates two random colors and a random angle (0–360°), then applies the gradient to a preview box. 


A simple and interactive Random Gradient Generator built using HTML, CSS, and JavaScript.

## 🚀 Features

- Generates random linear gradients
- Random angle between 0° and 360°
- Displays live gradient preview
- Copy CSS code to clipboard
- Responsive and clean UI
- Beginner-friendly project



## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (Vanilla JS)



## 📂 Project Structure

Random-Gradient-Generator/
│
├── index.html
├── style.css
└── script.js

---

## ⚙️ How It Works

1. Two random HEX colors are generated.
2. A random angle is generated.
3. A CSS `linear-gradient()` value is created.
4. The gradient is applied to the preview box.
5. The CSS code is displayed for copying.


## ▶️ How to Run the Project

1. Download or clone the repository.
2. Open the project folder.
3. Open `index.html` in your browser.
4. Click the **Generate** button to create a random gradient.
5. Click **Copy CSS** to copy the gradient code.

